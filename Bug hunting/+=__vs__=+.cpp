#include<iostream>
using namespace std;


int main(){
       int a = 10;
       int b = 10;
  
       // as we know += does, it add the value to it what is in right side we given
       a += 10;
       // a = a + 10;
  
  
       // but what happened when we do =+, 
       b =+ 10;

       cout << a << endl;
       cout << b << endl;
}
