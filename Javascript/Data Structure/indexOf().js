// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/indexOf


// indexOf take an element & return that element index.
// if element not found in array it will return -1.
// ** indexOf use strict equality.


const array = [10, 20, 120, 140, 23];
const idx = array.indexOf(120);
console.log(idx);
