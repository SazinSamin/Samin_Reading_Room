*** Number are not allowed as a first character of a variable, in this way javascript can identify the difference between number and
    a variable name.

*** Comparing two JavaScript objects always returns false.

*** toFixed(2) is perfect for working with money.


*** A common question is: How do I know if a variable is an array?
    The problem is that the JavaScript operator typeof returns "object":
 
 
*** JavaScript does not support arrays with named indexes. In JavaScript, objects use named indexes.
    If you use a named index, when accessing an array, JavaScript will redefine the array to a standard object.
    After the automatic redefinition, array methods and properties will produce undefined or incorrect results.



\'	single quote  
\"	double quote  
\\	backslash  
\n	newline  
\r	carriage return  
\t	tab  
\b	word boundary  
\f	form feed  



*** greater than equal (>=) or (<=) are comparison are not strickly equal.  

  
*** In switch, case values are tested with strict equality (===). 
